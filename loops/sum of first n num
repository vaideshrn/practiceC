#include <stdio.h>
void main() {
    int n, i, r;
    printf("enter the num:\n");
    scanf("%d", &n);
    i = 0;
    r = 0;
    while (i <= n) {
        r = r + i;
        i++;
    }
    printf("the sum is: %d", r);
}
