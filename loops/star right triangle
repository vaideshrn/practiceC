#include <stdio.h>
int main() {
    for (int i = 1; i <= 5; i++) { // Outer loop for rows
        for (int j = 5; j >= 6-i; j--) { // Inner loop for columns
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
