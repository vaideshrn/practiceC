#include <stdio.h>
void main() {
    int n, m;
    printf("Enter a number: ");
    scanf("%d", &n);
    m = n% 2;
    if (m==0) {
        printf("%d is an even number", n);
    }
    else {
        printf("%d is an odd number", n);
    }
}
