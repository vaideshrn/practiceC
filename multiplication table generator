#include <stdio.h>
void main() {
    int n, i, m;
    printf("enter the num for which you require a multiplication table:\n");
    scanf("%d", &n);
    for (i=1; i<=10; i++) {
        m =  n * i;
        printf("%d x %d = %d\n", n, i, m);
    }
}
